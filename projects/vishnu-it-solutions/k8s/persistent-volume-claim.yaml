apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: website-pvc
  namespace: dev

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: manual

  resources:
    requests:
      storage: 1Gi